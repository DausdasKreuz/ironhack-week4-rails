<% if flash[:alert] %>
    <div><%= flash[:alert] %></div>
<% end %>

<%= form_for @user do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <br>
  <%= f.label :email %>
  <%= f.text_field :email %>
  <br>

  <%= f.button %>
  <%= link_to "Cancel", user_path(@user) %>
<% end %>
