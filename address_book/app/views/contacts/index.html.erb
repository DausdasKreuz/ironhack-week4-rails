<h1>Contacts</h1>

<ul>
  <% @contacts.each do |contact| %>
    <li><%= link_to contact.name, "/contacts/#{contact.id}" %></li>
  <% end %>
</ul>

<%= link_to "Create a new contact", "/contacts/new" %>
